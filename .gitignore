node_modules/
dist/
tools/shots/

# Godot's import cache and build output. .godot/ is regenerated by
# `godot --import` on every machine and is large and churn-heavy; checking it
# in causes conflicts on every single commit.
godot/.godot/
godot/android/
build/
*.apk
*.keystore

__pycache__/
